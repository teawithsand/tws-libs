import React, { useMemo } from "react"

import { ImageCutterDemo } from "@app/display/misc/imageCutterDemo"

const IndexPage = () => {
	// const map = useMemo(() => mapDataFromLegacy(parseLegacyMapData(mapOne)), [])
	return (
		<main>
			<ImageCutterDemo />
		</main>
	)
}

export default IndexPage
